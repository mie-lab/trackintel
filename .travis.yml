dist: xenial
language: python

before_install:
  - sudo apt-get update
  - sudo apt-get install -y libspatialindex-c4v5 libspatialindex-dev

install:
  - pip install .
  - pip install pytest

script:
  - python -m pytest

after_success:
  - bash <(curl -s https://codecov.io/bash) -t edb4fbc4-a1d9-40b9-ab10-9c9b48e37df5