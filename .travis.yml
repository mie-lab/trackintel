dist: xenial
dist: xenial
language: python
python:
  - 3.6

# command to install dependencies

install:
  - pip install -r requirements.txt
  - pip install .
  - pip install pytest
# command to run tests
script:
  - python -m pytest